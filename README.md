# DexBinder v0.6A.1 — root icon path fix

This build matches the GitHub structure where the PWA icon PNGs live directly in the repository root.

Replace these three files only:
- index.html
- manifest.webmanifest
- sw.js

Keep the existing PNG files in the root:
- apple-touch-icon.png
- icon-192.png
- icon-512.png
- icon-maskable-512.png
